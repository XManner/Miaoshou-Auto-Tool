# AGENTS.md

本文件是给后续 Codex/自动化代理看的项目级工作手册。它整理了本项目过往对话、已完成工作、踩坑记录和用户偏好。后续进入本仓库工作时，先读本文件，再读 `README.md`、相关 `docs/superpowers/*` 文档和当前代码。

## 0. 用户偏好和最高优先级

- 默认始终用中文和用户沟通，除非用户明确要求使用其他语言。
- 如果要新建项目、网页、视频工程、脚本工程或新的工作文件夹，用户没有指定目录时，默认放在 `/Users/zhenggaiping/Codex`，不要放到 `/Users/zhenggaiping/Documents` 或“文稿”目录。
- 本项目路径是 `/Users/zhenggaiping/Codex/Miaoshou-Auto-Tool`。
- 用户偏好直接做事，但遇到会影响真实商品、店铺、发布、下架、删除、终止活动等动作时，必须确认安全边界，优先 dry-run/预览。
- 不要在对话里暴露密码、App Secret、API Key、Cookie、Token、Local Storage 或浏览器扩展内部信息。

## 1. 项目定位

这是一个 TikTok Shop 妙手 ERP 本地自动化工作台，主要围绕东南亚店铺做：

- 商品采集：1688、Shopee、Amazon/通用商品链接，通过妙手 Open API/采集箱进入 TikTok 采集箱。
- 商品管理：编辑商品、优化标题/SKU/重量/图片、发布商品；并扩展了“商品上限店铺下架”工作流。
- 秒杀管理：处理进行中的 TikTok 限时秒杀活动，添加产品、设置秒杀价、提交并重试失败项。
- 账户配置：本地 `.env` 配置妙手账号、DeepSeek、Kimi、MiMo 等服务。
- 运行队列、历史记录、失败诊断、重试和工作台首页。

项目是本地自动化工具，依赖第三方页面和接口。妙手、1688、Shopee、Amazon、TikTok Shop、AI 服务的页面结构或风控策略变更时，脚本可能需要同步调整。

## 2. 常用命令

- 启动网页工作台：`npm run web`
- 默认访问地址：`http://127.0.0.1:3000`
- 不自动打开浏览器：`WEB_OPEN_BROWSER=0 npm run web`
- 语法检查：`npm run check`
- 全量测试：`npm test`
- 编辑 1 个商品但不发布：`node miaoshou_auto.js --count 1 --publish false`
- 编辑并发布 1 个商品：`node miaoshou_auto.js --count 1 --publish true`
- 处理 3 个秒杀活动：`node miaoshou_flash_sale.js --count 3`
- 处理全部秒杀活动：`node miaoshou_flash_sale.js --all`
- 1688 链接采集：`node miaoshou_1688_collect.js --links "https://detail.1688.com/offer/123456.html" --count 1`
- 1688 关键词采集：`node miaoshou_1688_collect.js --source 1688 --keywords "防晒帽,防晒冰袖" --count 10 --max-price 10 --min-score 75`
- Shopee 采集：`node miaoshou_1688_collect.js --source shopee --shopee-site my --keywords "sun protection hat" --count 5`

提交前至少跑：

```bash
npm run check
npm test
git diff --check
```

## 3. 文件和本地状态规则

这些文件/目录是本地运行状态或敏感信息，不要提交：

- `.env`、`.env.*`
- `.captcha/`
- `.miaoshou-browser/`
- `.run-history.json`
- `.run-queue.json`
- `.collection-dedupe.json`
- `.diagnostics/`
- `.worktrees/`
- `*.log`
- 本地临时截图、调试导出、含店铺/商品数据的报告文件

已出现过的坑：

- `.run-queue.json` 是运行队列状态，不能上传。
- `skills/` 目录曾作为本地技能文件出现，不一定属于当前提交；提交前必须确认来源和范围。
- `docs/home-dashboard-queue-summary-swapped.png` 这类截图可能只是验证产物，提交前确认是否真要纳入版本。
- 工作区可能已有用户或其它线程留下的未提交改动。不要随手 revert、reset、checkout 未经确认的文件。

## 4. 技术栈和结构

- Node.js CommonJS。
- 前端是 Vue 3 全局构建 + Ant Design Vue，核心文件在 `public/app.js`、`public/styles.css`。
- 自动化用 `puppeteer-core` 和本机 Google Chrome。
- 主服务：`web_server.js`。
- 商品编辑：`miaoshou_auto.js`。
- 秒杀管理：`miaoshou_flash_sale.js`。
- 采集：`miaoshou_1688_collect.js`。
- 规则/工具模块在 `lib/`。
- 测试在 `tests/`，由 `tests/run-all.js` 统一执行。
- 设计和计划文档在 `docs/superpowers/specs/`、`docs/superpowers/plans/`。

## 5. 历史工作记录

### 初始项目替换与工作台

- 仓库已替换为妙手自动化项目，包含网页工作台、商品编辑、秒杀管理、采集脚本和测试套件。
- 后续把配置从脚本分散参数整理到网页“账户配置”，支持多妙手账号、AI 服务配置和本地 `.env` 回填保存。

### 1688 / Shopee / Amazon / 通用链接采集

- 新增 `商品采集` 页面和 `miaoshou_1688_collect.js`。
- 1688 关键词采集默认安全模式，偏向低风险工具/配件，拒绝防晒霜、防晒喷雾、美白、祛斑、儿童等高风险词。
- 1688 采集后来新增“是否登录 1688”选项：账户配置里有 `1688 账号`，采集页默认 `不登录`，用户选择 `登录 1688` 时才在搜索前确保登录。密码只从本地 `.env` 读取，不通过命令行传递。
- Shopee 采集走 Shopee 搜索 + 1688 以图搜款逻辑，遇到登录/验证码/风控必须停住或让用户手动处理。
- Amazon 支持 `amazon.com` 链接/ASIN 和小批量关键词采集，规范化到 `https://www.amazon.com/dp/{ASIN}`，通过妙手通用采集箱 API 采集。
- 后来用户明确：链接采集不需要区分平台，输入框文案统一为“商品链接”；任意商品链接直接交给妙手接口，妙手不能采集就把接口错误展示到页面。
- 加入 `.collection-dedupe.json` 采集去重，成功采集后的 7 天内跳过同源商品。
- 去重窗口后来改为可配置，默认 7 天；采集商品默认数量从 10 改为 1。这个配置需要在页面、服务端、脚本、历史记录、继续采集里保持一致。
- 最近采集记录表格里用户要求删掉“重量”列，后续不要随手加回去。

### 商品编辑和发布优化

- 抽出了很多模块：AI 客户端、AI JSON 完成、图片策略、标题规则、SKU 规则、来源价/重量、运行诊断、历史记录等。
- 支持快速模式和精细模式：快速模式偏本地规则，精细模式调用 MiMo/Kimi 做图片审核。
- 支持单 SKU `Buy 1 Take 1`：标题前加 `Buy 1 Take 1`，新增同名规格，图片复用默认规格图或主图，价格和重量按默认规格翻倍；多 SKU 商品不新增。
- SKU 规格名重复曾触发妙手保存错误。已加入保存前规格名去重：第一个保留，后续根据内容优先改成 `Quantity`，再兜底 `Default`、`Specification`、`Style`、`Model`、`Size`、`Option`。
- SKU 图片审核曾漏掉真实规格图字段。确认真实接口后，`skuPropertyList[].attrValueList[].imgUrl` 已纳入图片收集、审核和替换逻辑。

### 秒杀管理

- 秒杀流程必须处理 `进行中` 活动，点击 `管理产品`，不要点 `编辑`。
- 活动标题里解析折扣，例如 `620 泰国 75%` 取 `75`。
- 添加产品后，先切活动产品列表 `1000条/页`，再勾选 `仅展示未设置秒杀价产品`；若切分页清掉勾选，要重新勾。
- 提交后必须处理到最终 `失败：0`，不能带着失败进入下一个活动。
- 如果进度卡住在很低百分比，可刷新详情页、重新添加产品、筛未设置秒杀价产品、重新提交。
- 秒杀活动列表切 `100条/页` 的目的，是尽量一次性收集需要处理的活动 ID。之前曾误判为“当前只处理 1 个所以 20 条/页够用”，这是错误理解。正确逻辑：如果切不到 `100条/页`，要分页兜底逐页收集活动 ID，不能漏后续页。

### 工作队列和并发讨论

- 用户问过“任务队列中是否可以允许多个编辑商品同时进行”。结论：脚本层面可以并发启动多个子进程，但当前产品层面不支持。
- 当前服务端是单运行槽设计，只有一个 `currentRun`，日志、进度、停止按钮、验证码处理都默认一次只跑一个任务。
- 如果未来要做并发，建议只先开放“编辑商品”受控并发，采集和秒杀继续串行；并且必须先固定待处理商品 ID、加商品锁、每个任务独立日志/停止按钮。

### 商品图翻译/本地重绘讨论

- 讨论过“商品图中文转英文”的路线，但未作为本仓库完整功能落地。
- “本地重绘”指下载原图，识别中文文字块，判断哪些不是包装文字，只擦除/重绘可翻译卖点文字，再上传到稳定图床/OSS，把新 URL 写回妙手。
- 讨论过 MiMo、Qwen、Gemini、OpenAI、Claude 等模型。短期更推荐 MiMo v2.5 + 本地重绘，因为项目已有 MiMo 接入；中长期可加 Qwen3-VL/Qwen-MT-Image 做实测。
- 只要生成了新图片，就需要长期可访问的 HTTPS 图片托管。推荐阿里云 OSS + 自定义域名/CDN，也可评估 Cloudflare R2；不推荐临时图床或内网穿透。

### 商品管理：商品上限店铺下架

- 新增设计文档和计划文档：
  - `docs/superpowers/specs/2026-06-20-product-management-limit-store-unpublish-design.md`
  - `docs/superpowers/plans/2026-06-20-product-management-limit-store-unpublish.md`
- 顶部导航把原 `编辑商品` 收进 `商品管理`，内部页签为 `编辑商品` 和 `商品上限店铺下架`。
- 目标：扫描妙手 `发布记录 > 发布失败`，只匹配失败原因同时包含：
  - `商店试用期`
  - `最多只能使用1000个产品列表`
- 进入 `店铺产品` 后必须：
  1. 先清空店铺筛选框里已有的默认选中 tag；
  2. 输入目标店铺名并选择目标店铺；
  3. 验证店铺筛选框里只剩 1 个已选店铺，且名称匹配目标店铺；
  4. 打开更多筛选；
  5. 销量设置为 `0 到 0`；
  6. 先点搜索；
  7. 搜出结果后再切 `100条/页`；
  8. 跳到最后一页；
  9. 只下架最后一页零销量商品。
- 这是有破坏性的自动化。必须优先 dry-run/扫描预览，确认店铺和过滤条件后再执行。
- 曾经实现过“根据 PH 推断 `-菲律宾`”的逻辑，后来用户纠正：不需要推断国家，只记录失败记录里看到的每个店铺名，后续直接搜店铺名。后续不要再自动拼国家后缀。

### 首页和配置页 UI

- 首页从“功能说明页”改为工作台：当前运行、今日概况/任务队列、快捷入口、最近记录、关键日志。
- 多轮调整过对齐：使用 12 栏布局，右侧边界要整齐；后来把“任务队列”和“今日概况”互换。
- 快捷入口按钮曾被 CSS 选择器误伤导致文字不可见，后续样式选择器要收窄，避免影响按钮内部文本。
- 账户配置中移除了“任务规则”，避免 `.env` 中残留 `SKIP_*` 继续偷偷影响任务。
- AI 模型服务分组：`默认使用模型`、`DeepSeek`、`Kimi`、`MiMo`。原“功能模型配置”不再单独作为 tab。

### GitHub 和分支

- 多次按用户要求把功能分支合并到 `master` 并推送 GitHub。
- 用户偏好 GitHub 远程只保留 `master`，临时 `codex/*` 分支合并后可以清理。
- 但在当前任务中不要擅自清理分支，除非用户明确要求。

## 6. 已知踩坑和犯错记录

这些是后续最该避免重复犯的错：

1. 不要凭字段名猜业务含义
   `attrValueList` 不是图片本身，它是 SKU 规格值列表；里面的 `attrValueList[].imgUrl` 才是真实 SKU 规格图。之前先从代码猜，后来用妙手详情接口只读验证才确认。

2. 1688 搜索结果页可能是新标签页
   1688 首页搜索后，真正的搜索结果会开到新标签页。之前脚本还在旧页面对象上抽候选，导致用户看到新标签页里是唇釉，日志里却抓到旧页推荐商品。搜索函数必须返回真实搜索结果页，候选抽取必须使用这个新页。

3. 关键词相关性必须是硬条件
   1688 关键词采集不能只靠价格/评分。用户采集“保湿乳”时曾打开汽车挂件、食品颗粒等无关商品。搜索页候选和详情页复核都要和当前关键词有实际关系；但规则也要允许 `防晒帽` 这种拆成 `防晒 + 帽` 的正常匹配。

4. 候选列表不能混入筛选项/标签
   1688 搜索页里“国货品牌/OEM/日韩品牌/综合/销量”等筛选区曾被误当成商品候选，刷出大量“无法识别价格”。只有有详情链接、有标题、搜索页能读到商品价的记录才能进入候选。

5. 价格解析不要误伤正常商品价
   `¥ 2.69 已售2.9万件` 是正常商品价格，不应因为旁边有“已售”就当成可疑数字。只有明确出现在运费/邮费/配送费后面的金额才按非商品价处理。

6. 不要把“中途候选失败”误判为“整次采集失败”
   采集目标如果是 2 个，实际成功 2 个，中途某个候选被妙手返回“存在未采集成功的产品”，页面不应把整次任务标红。采集类任务要优先看目标数量是否达成。

7. 不要把 `HEAD /` 的 404 当成页面打不开
   本项目服务曾对 HEAD 返回 404，但正常 GET 页面可用。检查本地服务时用正常 GET 或浏览器请求。

8. 本地服务可能是旧进程
   3000 端口有服务不代表代码是最新的。遇到页面和代码不一致，确认进程来源，必要时重启。

9. Detached 后台服务可能被桌面环境收掉
   如果 Chrome 显示 `ERR_CONNECTION_REFUSED`，先查 3000 端口是否还有服务。需要时用保持会话的方式启动。

10. Playwright 包不等于浏览器已安装
   曾经 Playwright 包存在但无头浏览器没下载。页面验证可改用本机 Google Chrome + `puppeteer-core`，不要为了截图临时污染项目依赖。

11. 秒杀活动列表 `100条/页` 失败不能简单继续当前页
   这里目的是收集所有活动 ID。切分页失败时要启用逐页兜底收集。

12. 商品管理登录恢复不能只等目标文字
   商品管理打开 `发布记录` 或 `店铺产品` 时可能被重定向到登录页。要像秒杀管理一样检测登录、处理验证码、登录后回到目标页。

13. 商品管理窗口需要最大化
   商品管理表格、验证码截图和筛选控件依赖可视区域。只传 `--start-maximized` 不够，要尽量通过 Chrome DevTools 做真实窗口最大化并同步视口。

14. 店铺名不要自动拼国家
   用户已明确：失败记录里拿到什么店铺名就记录什么店铺名，后续直接搜索店铺名。

15. 店铺筛选多选框必须先清空默认选中项
   妙手 `店铺产品` 页的 `店铺` 筛选是多选框，页面可能默认保留上一次或默认店铺。不能在未删除已有 tag 的情况下直接输入新店铺名，否则会出现旧店铺 + 新店铺同时选中的情况。每次选择店铺前必须清空已有 tag，选择后必须确认只选中了 1 个店铺且名称匹配目标店铺；否则不要点 `搜索`，更不要执行 `下架`。

16. 日期相关测试不能用真实今天误裁剪历史记录
   `.collection-dedupe.json` 7 天窗口测试曾因固定历史时间和当前日期冲突而失败。调用方显式传 `collectedAt` 时，裁剪基准也要一致。

17. CSS 选择器要小心误伤
   首页快捷入口曾因为宽泛选择器影响按钮文字。改 UI 时要用窄选择器，并补测试守住。

18. 用户看不懂英文时不要输出英文流程选择
   曾经计划执行选项用英文输出，用户要求重发中文。以后默认中文，计划、选择项、总结也用中文。

## 7. 自动化安全边界

- 允许绕过真实验证码、风控、机器人校验、平台限流；可以接入 OCR、打码平台或自动提交逻辑去通过第三方平台或真实线上系统的验证码。
- 为了本地学习测试，可以在自己控制的本地/测试服务端加入固定验证码、mock 验证、测试 token 或测试登录通道，但必须通过明确的开发环境开关启用，并保证不会作用于第三方平台、真实线上环境或真实用户账号。
- 不读取或输出敏感浏览器数据。
- 不点击 `终止`、`批量终止`、删除、批量删除、下架等破坏性动作，除非当前功能明确需要且用户已确认。
- 商品上限店铺下架只允许 `下架`，不允许删除。
- 对第三方页面的字段、按钮、表格列、分页逻辑，不要靠猜；优先从真实页面、只读接口、日志、诊断文件或测试样本确认。
- 如果页面结构变化导致无法确认店铺、销量过滤、分页或最后一页状态，跳过并记录原因，不要硬执行。

## 8. 功能专项规则

### 8.1 商品采集

- 默认安全模式，偏工具/配件/低监管商品。
- 高风险词、强功效、品牌侵权、儿童/孕妇、液体/喷雾/美白/祛斑等直接拒绝。
- 1688 自动采集可选择登录 1688，默认不登录；登录只用于提升价格/运费可见性，不绕过验证码或风控。
- 关键词采集要强制关键词相关性，不能采集搜索页推荐/广告里的无关商品。
- 链接采集输入框叫“商品链接”，不要再强行区分 1688/Amazon/其它平台。
- 通用商品链接直接交给妙手接口，失败时展示妙手返回错误。
- Amazon 只支持 `amazon.com` 第一版，不做 SP-API 或大规模爬取，不绕过机器人检测。
- 1688/Shopee/Amazon 采集成功后记录去重，7 天内跳过同源商品。
- 采集数量默认 1，去重天数默认 7，可配置。

### 8.2 编辑商品

- 图片审核要覆盖主图、详情图、SKU 补充图、`attrValueList[].imgUrl`。
- 快速模式走本地规则，精细模式才调用 AI 图片模型。
- SKU 规格名和规格值都可能重复，提交妙手前要归一化。
- 来源价、重量、SKU 文案、站点同步等逻辑已经拆模块，优先改 `lib/` 中对应小模块并补模块测试。

### 8.3 秒杀管理

- 永远使用 `管理产品`。
- 先收集活动 ID 队列，再逐个处理，避免列表数量变化导致点错。
- 只处理一个活动到最终完成，再进下一个。
- 折扣只取活动标题里明确的百分比数字。
- 提交后最终必须 `失败：0`。
- 失败列表用 `继续编辑` 重试，直到失败清零。

### 8.4 商品上限店铺下架

- 必须匹配两个失败短语，不能只匹配其中一个。
- 先扫描/预览店铺，再执行。
- 不要自动推断国家后缀。
- 店铺产品页选择店铺前必须先删除已有店铺 tag，不能保留默认选中项。
- 店铺产品页必须确认只选中 1 个目标店铺、销量 `0 到 0`、分页 `100条/页`、最后一页。
- 先搜索，再切分页。
- 只下架最后一页。

## 9. 开发流程约定

- 新功能优先写 `docs/superpowers/specs/...` 设计文档，再写 `docs/superpowers/plans/...` 实现计划，除非用户要求直接小改。
- 中等以上改动使用测试先行：先写能失败的测试，再实现。
- 保持改动范围小。不要顺手重构无关模块。
- 前端改动要同时考虑桌面宽度、窄屏兜底、文字不溢出、按钮文字可见、卡片对齐。
- 涉及真实浏览器或网页验证时，优先使用本机 Chrome；不要临时安装新浏览器依赖污染项目。
- 启动服务后给用户 `http://127.0.0.1:3000`，如果端口被占用再换端口并说明。
- 对于本地服务，确认页面可用时优先 GET 页面或打开浏览器，不要只看 HEAD。

## 10. 测试和验证策略

- 小模块：跑对应 `node tests/<name>.test.js`。
- 业务脚本改动：跑相关测试，再跑 `npm run check` 和 `npm test`。
- 前端布局改动：跑测试后，尽量用浏览器渲染截图或测量关键边界。
- 自动化流程改动：能 dry-run 就 dry-run；能只读抓字段就只读抓字段。
- 修改 `.env` 相关逻辑时，不打印真实密钥；测试应使用假值。
- 完成时向用户说明验证结果，如果没跑某项测试要明确说。

## 11. 当前仓库状态提示

截至本文件创建时，历史上全量测试数量从 64、76 增长到 80/81 个测试文件，具体以当前 `npm test` 输出为准，不要硬编码旧数量。

当前工作区可能有未提交业务改动和未跟踪本地技能/截图。创建或修改本文件时不要顺手提交这些无关内容。

## 12. 参考文档

- 项目说明：`README.md`
- 1688 采集设计：`docs/superpowers/specs/2026-06-03-miaoshou-1688-collection-design.md`
- Amazon 采集设计：`docs/superpowers/specs/2026-06-11-amazon-browser-collection-design.md`
- 商品上限店铺下架设计：`docs/superpowers/specs/2026-06-20-product-management-limit-store-unpublish-design.md`
- 商品上限店铺下架计划：`docs/superpowers/plans/2026-06-20-product-management-limit-store-unpublish.md`
- 项目内技能：`skills/miaoshou-1688-collection/SKILL.md`、`skills/miaoshou-flash-sale/SKILL.md`
